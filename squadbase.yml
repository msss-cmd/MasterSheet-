version: '1'
build:
  framework: streamlit
  runtime: python3.11 # Ensure this matches your Python version
deployment:
  provider: gcp
  command: ["streamlit", "run", "app.py", "--server.port", "8080", "--server.address", "0.0.0.0"]
